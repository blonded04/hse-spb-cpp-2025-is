# Общее

Цель занятия:
1. Вспомнить про полиморфизм
2. Разобраться с синтаксисом множественного наследования, взглянуть на разные способы проектирования

Изменять можно только строчки между `TODO START`/`TODO END`, а также помеченные `TODO`.

Не забывайте про doctest (`doctest_main.cpp` в частности).

# Базовые упражнения (`0x`)
## `01-polymorphic-vector`
Уже реализована иерархия из двух классов-наследников (`IntValue`, `StringValue`) и одного базового абстрактного класса (`Value`).
Например, так могли бы представляться значения переменных в интерпретаторе языка Python.

Исправьте строчки в `main`: в вектор `v` должны вперемешку попасть значения (числа и строчки),
а в коде ниже все элементы вектора должны обработаться одинаковым образом.


## `02-employees`
### `TEST_BASIC`
Это и следующие упражнения — не независимые программы, а шаги [рефакторинга](https://ru.wikipedia.org/wiki/%D0%A0%D0%B5%D1%84%D0%B0%D0%BA%D1%82%D0%BE%D1%80%D0%B8%D0%BD%D0%B3)
и добавления новых функций в одну большую программу из нескольких единиц трансляции `employee*.cpp` в папке `problem`.

Чтобы начать очередное упражнение, вам требуется сделать все предыдущие.
В папке `solution` находится решение всех упражнений
К сожалению, частичных решений нет.

Читать все файлы необязательно, но рекомендуется.
Вот их описание:

* `employee.hpp` и `employee.cpp` — определения классов `Employee` (сотрудник), `Developer` (разработчик), `SalesManager` (менеджер по продажам).
  У каждого класса есть публичные поля и конструктор.
  Это единственные файлы, которые вам потребуется менять в упражнениях.
  Конструкторы можно оставить внутри `.hpp`, но остальные методы будут гораздо длиннее — их лучше вынести.
* `employee_test.cpp` — несколько групп тестов, в которых описаны требования к каждому упражнению.
  Рекомендуется прочитать.
* `employee_extra_tu.cpp` — ещё одна единица трансляции, включающая `employee.hpp`.
  Тогда если вы определите функцию в `employee.hpp`, то произойдёт нарушение ODR и вы получите ошибку линковки.
* `employee_enums.hpp`, `employee_enums.cpp` — объявления и определения enum'ов (перечислений) `ProgrammingLanguage` и `Region`,
  а также вспомогательных функций ввода-вывода.
  Это просто какие-то значения, которые можно задать разработчикам и менеджерам.

Интересный факт: заводить поля "имя" и "фамилия" для человека — [плохая идея в общем случае, категорически не соответствует реальности](https://github.com/kdeldycke/awesome-falsehood#human-identity).

Скомпилируйте все единицы трансляции вместе и убедитесь, что тесты из группы `#define TEST_BASIC` уже проходят.

Запрещается изменять код.

### `TEST_INHERITANCE`
Отрефакторите классы в `employee.hpp` и `employeese.cpp` так, чтобы `Employee` стал базовым классом для двух других.
В частности, требуется убрать дублирующиеся поля, но сохранить синтаксис вызова конструкторов.

В этом и следующих упражнениях вам разрешается менять только файлы `employee.hpp` и `employees.cpp`.

В каждом упражнении вы раскомментируете префикс `#define TEST_*` из `employee_test.cpp`
и должны заставить пройти все тесты.
В том числе с санитайзерами и под Valgrind.

Перед выполнением упражнения прочитайте новые тесты и убедитесь, что вы понимаете, что должно происходить.
**Только после этого** начинайте писать код.

### `TEST_POLYMORPHIC_VECTOR`
Сейчас у вас в коде может быть undefined behavior при удалении объектов через `unique_ptr<Employee>`
(на самом деле через `delete` от `Employee*`).

Убедитесь, что это так, раскомментировав очередной тест и запустив код под санитайзером и под Valgrind.
Если у вас macOS, то не обнаружится: там слабые санитайзеры и Valgrind.
Запустите под Ubuntu.

Избавьтесь от undefined behavior, добавив одну строчку в `employee.hpp`.
Если его не было — уберите одну строчку из `employee.hpp` и получите.

На предупреждение `cppcoreguidelines-special-member-functions` от `clang-tidy` можете пока забить.

### `TEST_PRINT_TO`
Добавьте к сотрудникам метод `printTo`, которая умеет выводить любого сотрудника в любой `std::ostream`.
Важно: где-то его придётся сделать чисто виртуальным.

### `TEST_OUT_OPERATOR`
Реализуйте `operator<<` для вывода сотрудников.
Вам должно хватить ровно одной реализации — незачем дублировать его для `Developer`/`SalesManager`.
Используйте метод `printTo`.

Строго говоря, виртуальный метод `printTo` стоило бы сделать `protected`, но тогда бы не получилось
написать тесты на предыдущее упражнение.

### `TEST_READ_FROM_SINGLE`
Добавьте для каждого неабстрактного класса статический метод `readFrom`, который возвращает
по значению нового сотрудника, считав его из `std::istream&`.
По сути этот метод можно было бы заменить на конструктор, но название `readFrom` лучше читается.

### `TEST_READ_FROM_FACTORY`
В языке C++ (да и во многих других языках) нет виртуальных конструкторов.
Поэтому если мы хотим считать сотрудника, не зная его тип, мы обязаны сделать для этого отдельную функцию,
которую иногда называют "фабричный метод".

Добавьте статическую функцию `Employee::readFrom`, которая сначала считывает из `istream&`
тип сотрудника, а потом вызывает соответствующий `readFrom` у нужного класса.
Возвращает сотрудника.

Вернуть сотрудника по значению не получится, потому что мы не знаем на этапе компиляции его тип.

### `TEST_NO_SLICING`
Сейчас ваш код наверняка подвержен слайсингу:
```
Developer d = ....;
SalesManager sm = ....;
Employee &e1 = d;
Employee &e2 = sm;
e1 = e2;  // WTF.
```
Запретите такой код: должно быть нельзя вызывать копирующие/перемещающие операции для базового класса `Employee`.
При этом важно сохранить возможность копировать и перемещать конкретных наследников `Developer` и `SalesManager`.

Советы:

* Не надо реализовывать четыре специальных метода руками, напишите `= default`, чтобы оставить их поведение по умолчанию.
* Вам достаточно поменять что-то у этих четырёх методов в базовом классе `Employee`.
* По сути вам требуется сделать так, чтобы их могли вызывать наследники (им же надо уметь копироваться/перемещаться), но не мог вызывать кто-то другой.

## `03-logger`
В файле `03-logger.cpp` сделано странное: при помощи множественного и виртуального наследования написано шесть реализаций логгера (сущность, которая пишет куда-то лог — журнал действий программы).
Логгер выводит строчку-сообщений по одному на строку: либо в `cout`, либо в `cerr`, либо в отдельный файл (в этом случае логгер владеет открытым файлом и закрывает его при своём уничтожении).
Также логгер может выводить сообщение либо просто так, либо дописывая перед ним некий "тег", который задаётся в момент создания логгера.
Всего шесть комбинаций.

Проверьте, что всё работает верно, скомпилировав решение и запустив `03-logger-test.sh`.

Используется множественное наследование и оно помогает избавиться от дублирования кода:
логика вывода содержится в `logger`, `cerr_logger`, `file_logger`,
а логика добавления тега — в `tagged_logger` (который по умолчанию выводит в `cout`).
Просто хранить `istream&` внутри `logger` нельзя: `file_printer` должен владеть файлом и закрыть его при уничтожении,
а остальные два логгера своим потоком не владеют.
Классы `tagged_cerr_logger` и `tagged_file_logger` используют множественное наследование
от `tagged_logger` и `cerr_logger`/`file_logger`, чтобы выводить сообщение с тэгом в нужное место.
Виртуальное наследование возникает, потому что `logger` в их иерархии встречается дважды.

Такой подход, несмотря на отсутствие явного дублирования кода, плохо масштабируется:
если у вас есть `N` способов вывести строку в лог и `M` способов её отформатировать,
вам надо написать `N*M` классов.
В этом примере `N=3`, `M=2`.

Используйте паттерн проектирования "мост", чтобы упростить код ([описание на Вики](https://ru.wikipedia.org/wiki/%D0%9C%D0%BE%D1%81%D1%82_(%D1%88%D0%B0%D0%B1%D0%BB%D0%BE%D0%BD_%D0%BF%D1%80%D0%BE%D0%B5%D0%BA%D1%82%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D1%8F)), [красивая картинка](https://github.com/kamranahmedse/design-patterns-for-humans/blob/e1a507ce14f9794bd02a5b309cc7f64d2b196219/README.md#-bridge)).
Вынесите способы вывода отформатированной строки в отдельную иерархию:
абстрактный класс `log_printer` и три реализации: `cout_log_printer`, `cerr_log_printer`, `file_log_printer`, всего `N+1` класс.
А в `logger` останется только логика форматирования:
абстрактный класс `logger` и две реализации: `plain_logger` и `tagged_logger`, всего `M+1` класс.
Класс `logger` хранит внутри себя экземпляр кого-то из `log_printer` и обращается к нему — это такой "мост" между двумя иерархиями.
Таким образом получается `N+M+2` класса, что асимптотически меньше `N*M`.

Классы `plain_logger` и `logger` можно было бы объединить вместе, но тогда бы не было красивого разделения на "абстракции" и "конкретные реализации".
В будущем бы наверняка возникли проблемы, как в `02-employees-level`.

## `30-employees-protected-ctors`
Возьмите своё решение `01-employees` и избавьтесь в нём от фиктивных параметров,
которые вы передаёте в конструкторе `LeadDeveloper` конструкторам `Developer` и `Manager`.
Для этого придётся создать им новые конструкторы и ещё чуть-чуть повозиться.

В итоге должно быть невозможно создать кривого `Developer`/`Manager` с фиктивными параметрами.
Скорее всего вам придётся сделать фиктивный конструктор для `Employee` — не надо его определять,
чтобы его было невозможно вызвать.
