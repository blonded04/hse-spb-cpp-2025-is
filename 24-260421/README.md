# Общее
Цель занятия: подготовиться к домашке по написанию `unique_ptr` и `shared_ptr`.

Все упражнения необходимо запускать либо под Address Sanitizer (`-fsanitize=address` при компиляции и линковке), либо под Valgrind.
Иначе не получите кучи полезных сообщений об утечках, double free и прочим.

Перед запуском упражнения убедитесь, что вы понимаете, почему текущее решение неверное (даже если компилируется).

Разрешается изменять только код, помеченный `TODO`.

# Упражнения
## `01-string`
Это упражнение на правило пяти и move-семантику.
Реализуйте класс `string`, похожий на кусочек `std::string`: сишная строка с семантикой копирования/перемещения.

Уже есть заготовка и несколько тестов, но они почему-то падают. Исправьте.

## `02-stack`
Это упражнение на шаблоны классов.
Напишите шаблон класса `stack<T>`, который поддерживает операции `push`/`pop`, а внутри использует `vector<T>` для хранения данных.

Реализацию напишите целиком в `stack.h` — это стандартная конвенция.
Так как шаблон нельзя скомпилировать заранее, не зная точный тип, определения (почти) всех шаблонов пишутся в заголовках.
На ODR это не влияет.

## `03-stack-move-only`
Это упражнение на шаблоны классов.
Скорее всего, ваше решение `02-stack` плохо работает с move-only типами, из-за этого это упражнение не компилируется.
Исправьте.

## `04-shared-ptr`
Пусть мы пишем сервер для некоторой игры.
К серверу подключаются-отключаются игроки (класс `player`), причём каждый игрок может либо начать новую игру (конструктор от одного аргумента),
либо присоединиться к другому игроку (конструктор от двух аргументов).
В первом случае создаётся новое поле из десяти точек, во втором игрок присоединяется к тому же полю, на котором играет его коллега.
На одном поле может играть сколько угодно игроков.

Так как игроки могут отключаться в случайном порядке, нельзя выделить какого-то одного "владельца" поля.
Можно было бы хранить поля в каком-нибудь `map`, считать количество активных игроков на каждом поле и удалять, когда потребуется,
но почти это же можно сделать при помощи `shared_ptr`.
Попросите практика нарисовать, как он работает и примените его в задаче.

Картинка: https://docs.microsoft.com/en-us/cpp/cpp/how-to-create-and-use-shared-ptr-instances?view=msvc-170

При этом `weak_ptr` не требуется.
